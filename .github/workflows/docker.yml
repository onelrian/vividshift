name: Run Docker Container

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '0 0 */14 * *'  # Runs every two weeks

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Image
        run: |
          docker build -t ghcr.io/${{ github.repository }}/my-image:latest .
          docker push ghcr.io/${{ github.repository }}/my-image:latest

      - name: Run Docker Container and Save Output
        run: |
          # Run the container and capture output in output.txt
          docker run --rm ghcr.io/${{ github.repository }}/my-image:latest | tee output.txt

      - name: Debug Files in Workspace
        run: ls -lah

      - name: Debug Output
        run: |
          # Check if output.txt exists and is not empty
          if [ ! -s output.txt ]; then
            echo "Error: output.txt is missing or empty!"
            exit 1
          fi
          cat output.txt

      - name: Upload Output as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: output
          path: output.txt
